/**
 *  * main - abc
 *   * Return: 0 Always (Success)
 *    */
int main(void)
{
	int a;
	
	for (a ='0' ; a <= '9' ; a++)
	{p
		utchar(a);
		if(a != 9)
		{putchar(',');
			putchar(' ');
		}
	}
	putchar('\n');
	return (0);
}
